<%= form_with(model: suggestion, local: true) do |form| %>
    <%= hidden_field_tag :dpt, params[:dpt] %>

  <% if suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
        <% suggestion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.hidden_field :item_id,value: item.id %>
  </div>

  <div class="field">
    <%= form.label :title; "Titre de l'offre" %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content, "Description de l'offre" %>
    <%= form.text_area :content %>
  </div>
  <div class="field">
    <%= form.label :start_at, "Début de l'offre" %>
    <%= form.date_field :start_at %>
  </div>
  <div class="field">
    <%= form.label :end_at; "Fin de l'offre" %>
    <%= form.date_field :end_at %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
